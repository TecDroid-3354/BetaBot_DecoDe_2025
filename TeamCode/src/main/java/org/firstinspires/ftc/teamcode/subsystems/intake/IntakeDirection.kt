package org.firstinspires.ftc.teamcode.subsystems.intake

// The IntakeDirection enum defines the right & left intake as seen from above the turret's motor
enum class IntakeDirection {
    RIGHT,
    LEFT
}